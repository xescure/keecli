# FX CLI Example Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env files to version control!

# Environment Variable Support
# The CLI reads environment variables with the FX_CLI_ prefix
# Use double underscores (__) for nested options

# User Authentication
# Your KeetaNetwork passphrase for deriving account keys
FX_CLI_PASSPHRASE="your secret passphrase here"

# Resolver Configuration
# The public key string of your FX resolver account
# This is obtained when you set up your FX service
FX_CLI_RESOLVER="0x06abc123def456..."

# Token Configuration for Swaps
FX_CLI_FROM="$TOKEN_A"
FX_CLI_TO="$TOKEN_B"
FX_CLI_AMOUNT="1000"
FX_CLI_AFFINITY="from"

# Token for Conversions Command
FX_CLI_TOKEN="$TOKEN_A"

# Common Token Identifiers (for reference)
TOKEN_A="$TOKEN_A"
TOKEN_B="$TOKEN_B"
TOKEN_C="$TOKEN_C"

# Network Configuration (optional)
# Defaults to "test" if not specified
FX_NETWORK="test"

# Usage Examples with Environment Variables:
#
# After setting environment variables above, you can run commands without flags:
#
# List tokens:
# ./fx-cli.sh list-tokens
#
# List conversions:
# ./fx-cli.sh list-conversions
#
# Execute swap:
# ./fx-cli.sh swap
#
# Request faucet tokens:
# ./fx-cli.sh faucet
#
# You can still override environment variables with command line flags:
# ./fx-cli.sh swap --amount 2000 --to "$TOKEN_C"
#
# Or mix environment variables with command line arguments:
# export FX_CLI_PASSPHRASE="your passphrase"
# export FX_CLI_RESOLVER="0x06abc123..."
# ./fx-cli.sh list-tokens  # Uses env vars
# ./fx-cli.sh swap -f "$TOKEN_C" -t "$TOKEN_D" -a "500"  # Overrides some values
